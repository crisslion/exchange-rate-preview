import * as React from "react";
import * as ReactDOM from "react-dom";
import { Color } from "../constants/colors";

export class HelloWorld extends React.PureComponent<{}> {
    render() {
        return (
            <h1>Hello typescript basic template!</h1>
        );
    }
}
